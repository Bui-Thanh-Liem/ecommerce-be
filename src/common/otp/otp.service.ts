export class OrderService {
  sendOtp(phoneNumber: string, otp: string): void {
    // Implement the logic to send OTP to the provided phone number
    console.log(`Sending OTP ${otp} to phone number ${phoneNumber}`);
  }
}
